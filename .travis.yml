language: python
python:
 - "3.5"
cache: pip
services:
  - postgresql
install:
 - pip install -r requirements.txt
 - pip install coveralls
before_script:
   - psql -c 'create database test_db;' -U postgres
script:
 - python manage.py
 - pytest
 - pytest --cov=app
after_success:
 - coveralls
  